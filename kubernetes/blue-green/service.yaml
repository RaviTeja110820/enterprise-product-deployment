apiVersion: v1
kind: Service

metadata:
  name: enterprise-service
  namespace: enterprise

spec:

  type: LoadBalancer

  selector:

    app: enterprise
    version: blue

  ports:

  - port: 80
    targetPort: 8080